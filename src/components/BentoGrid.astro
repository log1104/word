---
import BentoTile from './BentoTile.astro';

interface Props {
	posts: any[];
}

const { posts } = Astro.props;
---
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
	{posts.map((post, index) => (
		<BentoTile post={post} index={index} />
	))}
</div>
